# AndroidX usage
android.useAndroidX=true
android.nonTransitiveRClass=true
# Enable Kotlin incremental compilation
kotlin.code.style=official

# JVM arguments to prevent OOM
org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
